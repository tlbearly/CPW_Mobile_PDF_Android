<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.example.tammy.mapviewer.PDFActivity">
    <item
        android:id="@+id/action_deleteAll"
        android:icon="@android:drawable/ic_input_add"
        android:title="@string/deleteAllDBrecords"
        android:visible="true"
        app:showAsAction="never" />
    <item
        android:id="@+id/action_open"
        android:icon="@android:drawable/ic_input_add"
        android:title="@string/open_pdf"
        android:visible="true"
        app:showAsAction="never" />
    <group android:checkableBehavior="all">
        <item
            android:id="@+id/action_quality"
            android:orderInCategory="100"
            android:title="@string/action_quality"
            app:showAsAction="never" />
    </group>
</menu>
